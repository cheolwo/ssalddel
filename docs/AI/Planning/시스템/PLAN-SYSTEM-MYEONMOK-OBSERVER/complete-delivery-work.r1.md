# 가상 배달 전체 연결 작업 명세 r1

사용자의 2026-09-07 `그냥 끝까지 구현해 줘`를 기존 승인 표본 전체의 구현 요청으로 결속한다. 담당은 `01a06f87-43c4-7150-8138-3368d9744af5`. 기준은 synthetic-observer.r2와 Accepted synthetic-spatial-study.r1이며 실제 자료·정산·우천·다른 영역은 제외한다.

## WI별 구현 경계

| 작업 | 행위 주체 → 대상 | 직접 결과 | 실패·회복·다음 선택 |
| --- | --- | --- | --- |
| 기사 배정 | 가상 배차 규칙 → 대기 기사·조리 주문 | 기사 1명에 주문 하나 결속 | 도달 불가면 대기, 재개 후 같은 주문 |
| 이동 | 기사·차량 → 명시적 도로/출입구 | 다음 위치·거리 | 정지 시 보존, 같은 경로 재개 |
| 픽업 | 음식점 → 기사 | 조리 완료·출입구 도착 후 물품 인계 | 조리 미완료면 대기, 완료 후 인계 |
| 전달 | 기사 → 주택 출입구 | 인계 1 Tick 뒤 전달 완료 | 미도착이면 불가, 다음 수령 |
| 수령 | 주문자 → 전달 주문 | 다음 Tick 수령 확인 | 중복 효과 없음, 다음 복귀 |
| 복귀 | 기사 → 대기점 | 기사 가용 상태 복원 | 미복귀면 다음 묶음 금지 |

## E7 수직 작업 명세 v2 영향 검토

Logic E7→E1: 반복 배송·재진입(E7), 차단·중복·과거 저장(E6), 공통 Runtime 결속(E5), 동결 합성 경로(E4), 결정성·저장/재생(E3), 기존 주문 상태·Tick Adapter(E2), 기사/차량/주문 안정 ID(E1).

Presentation E7→E1: 전체/따라가기 관찰(E7), 정지·재개 판독(E6), canonical Scene에 표현 결속(E5), 승인 기본 도형·출입구(E4), 표현 시험(E3), 상태 사본 읽기(E2), 표현은 권위가 아님(E1).

구체 기사·차량은 새 합성 프로필의 고정 ID, 음식점·주문자는 기존 주문 계약의 ID로 식별한다. 영속 권위는 공통 Session이며 저장된 생성 요청·Tick 명령의 결정적 재생으로 같은 주체/주문을 재구성한다. 구현 전 대장/명세 관문을 결속하고 E 승격은 실제 증거 뒤 별도로 판정한다.

## 수정 범위와 완료 조건

신규 합성 배달 Core partial/계약, 기존 Session의 한 Tick·사본·복제·해시 연결, 전용 표본·시험, 별도 Unity 프로필·Controller/표현만 수정한다. 공용 파일은 patch 직전 재확인한다. 기존 수동 표본·100 Tick 제한·기존 저장 규칙은 보존한다. 새 프로필만 충분한 실행 시간을 명시하고 자동 주문은 두 집 수령·복귀 후 생성한다.

완료는 두 집 배송과 복귀·다음 묶음, 단일/합산 Tick 결정성, 저장 재진입, 정지와 오류 처리, Unity 컴파일/표현 연결 검증이다. 실제 Game View·Hosted는 수행 여부를 별도로 남긴다. 준비·지원 모듈만으로 전체 완료를 선언하지 않는다.
