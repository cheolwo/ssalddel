# 음식점 조리 대기열 구현 명세

승인 기준: `restaurant-npc-operation.r3`, SHA256 `410C8CF0501C1256C5ACA39BF311A21B46B559F95FE373BF96ECD30F3E4ECF08`.

- 직접 책임: 수락한 주문을 접수 시각·안정 ID 순으로 빈 조리 자리에 배정하고 음식 준비까지 연결한다. 자동 수락 WI와 배차는 별개다.
- 준비 주체: 기존 Ready NPC와 권위 대상 주체. 실제 주문·시설은 기존 스냅샷 ID로 해석한다.
- 정책: 기존 NPC 정책에 선택형 조리 자리 수와 조리 시간 변경을 추가한다. 미설정 과거 정책·저장 재생은 그대로 유지한다. 조리 정책이 없으면 자동 수락 이후 대기한다.
- 정책 변경은 아직 시작하지 않은 주문부터 반영한다. 진행 중 작업의 시간·자리 점유는 변경하지 않는다. 자동 처리 해제도 진행 중 작업을 취소하지 않는다.
- E7→E1: 실제 관찰 완주·입력·배치(E7~E5)는 후속, 판독 후보(E4)는 조리 대기/조리 중/준비, 회귀(E3)는 FIFO·자리 제한·정책 변경·Save/Replay, 결속(E2)은 기존 정책 포트·작업 상태, 규칙(E1)은 권위 Core만 작업 확정.
- E1→E3 시험 후에도 실제 Unity·Hosted·E 승격을 선언하지 않는다. 공간·자산 변경 없음으로 전문 공간 연구는 이번 범위에 해당하지 않는다.
- 조리 권한·정책 시설 일치와 세션 종료 전 완료 가능성을 검사한다. 별도 입력에 따라 작업을 진행시키는 기존 일반 Task 실행을 재사용한다.
- 저장: 정책 자리 수·변경 명령, 작업 일정·조리 시간·주문 revision을 같은 재생 결과에 포함한다. 실행 중 개인 자원·식재료 소비를 추가하지 않는다.
