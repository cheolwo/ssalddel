# H1 생활주택 Blender 손상 파생형 — 2026-09-03

## 결과

한스 집을 전용 공간 유형으로 새로 만들지 않고 기존 `h1-stock:farm-residential-home` **농장 생활 주택 H1**의 특정 배치 인스턴스로 유지했다. 보유 Synty Farm 후보 두 개를 같은 Blender 카메라로 비교한 뒤, 작고 단층이라 Farm 경계의 생활주택으로 읽히는 `SM_Bld_Farmhouse_02.fbx`를 기준 원본으로 선택했다.

원본 파일은 수정하지 않았다. 프로젝트 소유 복사본에만 숲길 사선에서 읽히는 여러 곳의 작은 지붕 결손과 노출 판재를 추가하고, 불균일한 갈색 먼지층으로 오래 사용한 외관을 표현해 `.blend` 및 FBX로 분리했다. 이 작업의 정식 명칭은 **비파괴 파생 자산 제작**이며, 손상 형태는 **손상 상태 베리에이션(damaged-state asset variation)**, 갈색 먼지·사용감 표현은 **풍화 처리(weathering)**다.

## 원본과 계보

- H1: `h1-stock:farm-residential-home` revision 2
- Synty 팩: `PolygonFarm`
- 기준 원본: `Assets/Synty/PolygonFarm/Models/SM_Bld_Farmhouse_02.fbx`
- Unity `.meta` GUID: `ea1ba4b74f4bce84d977ef4f2cdaceb3`
- 원본 SHA-256: `8DBFC93740CF59125FCA4263334C408E81AFD55C03291A07335B07E9476A58D7`
- 파생형 ID: `farm-residential-home-damaged-variant`
- VisualKey: `FarmResidentialHome.Damaged.ScatteredRoofHolesAndDust`
- 사용자 판독 차이: 생활 가능한 주택 실루엣과 출입구를 유지하면서 곳곳의 작은 지붕 결손, 노출 판재와 불균일한 갈색 먼지 풍화가 보인다.

## 산출물

- 작업 명세: `C:/Users/user/ssalddel/ArtSource/Blender/workflows/farm-residential-home-damaged-variant.r1.json`
- Blender 원본: `C:/Users/user/ssalddel/ArtSource/Blender/source/farm-residential-home-damaged-variant/farm-residential-home-damaged-variant.blend`
- 프로젝트 소유 FBX: `C:/Users/user/ssalddel/ArtSource/Blender/exports/farm-residential-home-damaged-variant/farm-residential-home-damaged-variant.fbx`
- 검증 결과: `C:/Users/user/ssalddel/ArtSource/Blender/validation/farm-residential-home-damaged-variant/result.json`
- 비교 렌더: 같은 검증 폴더의 `before.png`, `after.png`, `damage-closeup.png`

Blender 파일은 공급사 원본 참조 컬렉션과 프로젝트 소유 파생 컬렉션을 분리한다. 내보내기에는 파생 메시만 포함하며 공급사 참조 컬렉션은 포함하지 않는다.

## 검증

- Blender 5.2.1 LTS에서 작업 파일 저장 뒤 별도 프로세스로 재열기: 통과
- 프로젝트 소유 FBX를 새 Blender Scene에 다시 가져오기: 통과
- FBX 왕복 결과: 메시 객체 12개, 원본과 같은 전체 크기 `8.5844 × 9.2089 × 6.7943`
- 손상 객체: 기존 손상과 사용자가 표시한 중앙·우측·하단 지점을 포함한 지붕 공동 6개와 노출 판재 3개
- 풍화 재질: 프로젝트 소유 파생 재질에만 절차적 갈색 먼지층과 `0.90` 거칠기를 적용
- 원본 FBX 해시 재확인: 변경 없음
- 파생 명세 계약: 통과
- 공통 명세 회귀: 7/7 통과

## 증거 상한

현재 결과는 **Blender 파생 자산·렌더 증거**다. Unity 후보 폴더로 가져오기, Prefab 조립, Collider·Bounds·출입구·통행 검사, 배치 맵 결속, Scene 배치, Play Mode, Game View와 E5는 수행하지 않았다. 따라서 `BlenderValidatedCopy`까지만 성립하며 Presentation E5나 실제 한스 집 배치를 선언하지 않는다.
