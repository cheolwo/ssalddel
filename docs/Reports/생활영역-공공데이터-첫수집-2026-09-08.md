# 생활 영역 공공데이터 첫 수집 — 중랑구 전통시장

- 기준: 2026-09-08, [생활 여덟 영역 계획 r1](../AI/Planning/자료/PLAN-DATA-EIGHT-LIFE-DOMAINS-001/README.md)의 후속 사용자 요청(로그인 포털 이용·기존 Docker DB 저장·계속 진행).
- 판정: **실자료 12곳 저장 및 독립 재조회 완료 / 비공개 검토보류 / 여덟 영역 전체 수집 아님**.
- 범위: 먹거리·장사·생활 장소를 함께 다루는 첫 작은 묶음. 기존 동네 조사와 연결하기 위해 중랑구를 선택했다. 생활 영역 기획 전체나 신규 게임 규칙의 승인으로 확대하지 않았다.

## 원천·수집

공공데이터포털의 로그인 상태와 [전국전통시장표준데이터](https://www.data.go.kr/data/15012894/standard.do)를 실제 브라우저에서 확인했다. 소재지도로명주소 `중랑구` 조회 결과는 12곳이었다. JSON 내려받기 동작에서 관찰한 공식 다운로드 주소를 사용했다. 이 주소는 화면 필터와 달리 전국 1,393행을 반환한다. 따라서 **원본 1,393행 확보와 중랑구 12행 DB 반입을 구분**한다. 전국 전체 반복·정기 수집을 시작하지 않았다.

- 원본 JSON: 783,788 bytes, SHA256 `13FFD04A946EC7EEC28222C8C3762F2E77AAB919CD250888CDAAF7F51CCE303B`.
- 수집 시각: 2026-09-08 17:38:29 KST. 12행 데이터 기준일은 모두 **2025-11-10**. 현재 영업·점포수 확정이나 실측 좌표가 아니다.
- [공식 DCAT](https://www.data.go.kr/biz/dcat/metadata/15012894.do): `이용허락범위 제한 없음`을 직접 확인했다. 원천·메타데이터는 로컬에 보존하며 외부 게시·사진 수집·유료 호출은 하지 않았다.
- 홈페이지는 수정일 2025-11-26·연간을 표시하지만 [schema.org 메타데이터](https://www.data.go.kr/catalog/15012894/standard.json)는 2021-08-02·수시를 반환했다. 이 차이를 보존하고 최신 기준일로 임의 치환하지 않는다.
- 응답 Content-Type은 `text/html; charset=UTF-8`이나 실제 본문은 JSON 배열이다. 원응답 헤더는 수집 영수증에 남기고 파싱 검증 후 DB 형식만 `application/json`으로 기록했다.
- 처음 확인한 상가(상권)정보 15083033은 최신 파일·이용조건 확인까지이며, 이번 DB 반입 자료와 다르다.

원본과 계보: `artifacts/local/public-data/eight-life-domains/market-20260908-r1/`의 `markets.json`, `metadata.json`, `metadata.rdf`, `acquisition.json`. 인증 토큰·쿠키·API키는 추출하거나 저장하지 않았다. 이번 공개 내려받기 주소에는 인증키가 필요하지 않았다.

## 저장 결과

기존 `hongdal-mysql-1 / 127.0.0.1:13306 / hongdal_dev`의 일반 계정·Compose 프로젝트·포트·작업 경로를 확인했다. 기존 ASP.NET 프로젝트의 `평창군공공공간원본등록Service`와 `EfExternalDataIngestionStore`를 작은 로컬 가져오기 도구에서 재사용했다. HTTP 서버·migration·새 DB·새 테이블은 시작하지 않았다.

| 구분 | 실제 결과 |
| --- | --- |
| 원본 계보 | `public_data_raw_snapshots` ID 12, 원본 파일 hash·크기·비공개 경로 |
| 조회 가능한 자료 | `public_data_normalized_records` ID 79421~79432, 12행 |
| 출처 / 자료 | `semas-traditional-market-status` / `data-go-kr-15012894-standard` |
| 검토 상태 | 전부 `PendingHumanReview`, 원본 수집 Run은 `Partial` |
| 첫 반입 | 추가 12 / 기존 0 |
| 동일 입력 재반입 | 추가 0 / 기존 12, 원본 계보 중복 없음 |
| 독립 재조회 | 별도 EF 연결 및 Docker MySQL 클라이언트에서 12행 확인 |
| 공개 시장 원장 | `public_data_traditional_markets` 0행 전후 유지 |

시장명·시장유형·두 주소·개설주기·위경도·점포수·취급품목·상품권·홈페이지·화장실·주차장·개설연도·공개전화·기준일·제공기관의 원문 18필드를 각 행 `TextValue` JSON으로 보존했다. 빈 항목은 빈 값으로 남긴다. 시장별 공개 고유번호가 이 파일에 없어 이름+도로명주소의 파생 hash를 명시한 ID를 사용했다. 공식 고유번호로 위장하지 않는다. 날짜 전용 자료를 UTC 자정에 정규화하지만 실제 관측 시각으로 해석하지 않는다.

| 시장 | 원천 점포수 |
| --- | ---: |
| 면목골목시장 | 96 |
| 동원시장 | 14 |
| 중랑교종합상가 | 64 |
| 동원전통종합시장 | 114 |
| 우림골목시장 | 123 |
| 면목시장 | 33 |
| 동성타워프라자 | 75 |
| 장미제일시장 | 57 |
| 신아타운 | 165 |
| 사가정시장 | 137 |
| 중랑동부시장(동부골목시장) | 105 |
| 태능엔터피아 | 41 |

## 코드·검증

- 신규 도구만: `eng/Ssalddel.PublicDataPortalImport/`의 프로젝트·Program·전통시장표본Parser·acquire.ps1. 기존 서버 서비스/공개 API/SourceCatalog/게임 코드 수정 없음. 이 표준자료의 새 DatasetId를 자동 수집·공개 카탈로그에 연결하지 않았으며 현재는 검토용 저장·조회 대상이다.
- 자체검사 11개: 실제 표본 투영, 결정성, 빈 목록, 범위 밖 제외, 배열형식·중복·기준일·기관·좌표·점포수·필수필드 오류 거부. 이 결과는 일반 전통시장 파서 전수 인증이 아니라 정확 동결 입력의 제한 도구 검사다.
- 실제 DB 반입 전 preview 0행 → apply 12행 → 재apply 추가0 → verify 12행. 파생 18필드 JSON을 원본과 전부 대조했다. 별도 MySQL 조회로 이름·점포수·기준일·상태·원본 hash도 확인했다.
- 첫 빌드에는 상태코드 이름 오타로 CS0103 한 건이 있었고 수정 후 빌드·자체검사를 통과했다. 실패 당시 DB 쓰기 없음.
- 범위 Fast `20260908-174135`는 도구 폴더를 guidance-only로 분류하여 diff만 검사했다. 이를 코드 시험으로 세지 않고 직접 도구 빌드/자체시험·실제 DB 검사로 보완했다.
- 기존 수집 기반 회귀 `ExternalDataIngestionFoundationTests` **16/16 통과**, 건너뜀0. 해당 시험 빌드도 성공했다. 전체 서버 시험이나 웹 서비스 가동 검증은 아니다.
- 범위 Task `20260908-174318`도 guidance-only로 분류되어 diff 검사만 통과했다. 실제 코드 증거는 위 별도 도구 빌드·자체시험·기반회귀·DB 재조회이다.
- 상세 결과: `artifacts/local/validation/public-data-markets-20260908/`의 apply/reapply/verify/self-test JSON, `independent-readback.txt`, `foundation.trx`, `foundation-tests.log`.

## 남은 범위

다음 후보는 먹거리 가격(KAMIS/aT 등)의 현재 이용권한·기존 수집기와 주거/도로 자료 재사용이다. 키나 신규 활용신청이 실제로 필요할 때 해당 자료의 조치만 사용자에게 요청한다. 이번 자료의 현재 영업 검토·공식 시장번호 결속·공개 서비스 반영·게임 적용·팔정 전체 수집은 미완료다. 기존 앱 컨테이너의 재시작 상태는 관찰했으나 이번 DB 연결은 정상이며 앱 재배포·복구는 하지 않았다. Unity·Scene·Save·commit·push 없음.
